# NBU AppZone server

## Requirements

* sbt
* mongodb

## Build & run
    cd /vagrant
    ./sbt
    > container:start
    > ~ ;copy-resources;aux-compile

Now open the site's [root page](http://localhost:8080/) in your browser.